<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Detalhes da Venda</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">

  <!-- Estilo embutido -->
  <style>
    body {
      background-color: #b84f06; /* Cor de fundo principal */
      color: white;
    }

    h1 {
      color: #ffae67; /* Cor para o título */
      font-size: 2rem;
      text-align: center; /* Centraliza o título */
      margin-top: 20px; /* Espaçamento acima do título */
    }

    .btn-outline-primary {
      background-color: #ca671e; /* Cor de fundo do botão */
      color: white;
      border: none;
      transition: background-color 0.3s ease;
    }

    .btn-outline-primary:hover {
      background-color: #dc7f37; /* Cor ao passar o mouse */
    }

    p {
      font-size: 1.2rem;
      margin: 0; /* Remove margem vertical extra */
    }

    strong {
      color: #ffae67;
    }

    .container {
      margin-top: 20px;
      padding: 10px 15px;
      background-color: #512a1b; /* Cor de fundo dos detalhes */
      border-radius: 8px;
      width: fit-content; /* Ajusta o tamanho conforme o conteúdo */
      margin-left: auto; /* Centraliza o contêiner na tela */
      margin-right: auto; /* Centraliza o contêiner na tela */
    }

    .btn-wrapper {
      margin-top: 20px; /* Espaço acima do botão */
      text-align: center; /* Centraliza o botão dentro do contêiner */
    }
  </style>
</head>
<body>
  <%- include('../partials/menu') %>

  <h1>Detalhes da Venda</h1>

  <div class="container">
    <p><strong>Produto:</strong> <%= venda.produto %></p>
    <p><strong>Quantidade:</strong> <%= venda.quantidade %></p>
    <p><strong>Valor:</strong> R$ <%= venda.valor.toFixed(2) %></p>
    <p><strong>Fornecedor:</strong> <%= venda.fornecedor ? venda.fornecedor.nome : 'Não informado' %></p>
    <p><strong>CNPJ:</strong> <%= venda.fornecedor ? venda.fornecedor.cnpj : 'Não informado' %></p>
    <p><strong>Data da Venda:</strong> <%= venda.dataVenda ? venda.dataVenda.toLocaleDateString('pt-BR') : 'Data não informada' %></p>

    <div class="btn-wrapper">
      <a class="btn btn-outline-primary" href="/vendas">Voltar</a>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
