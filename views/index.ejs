<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sistema de Estoque</title>
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Estilo embutido -->
    <style>
        /* Estilos gerais */
        body {
            background-color: #b84f06; /* Cor de fundo principal */
        }

        .text-white {
            color: white;
        }

        /* Estilos para os botões */
        .btn-color2 {
            background-color: #ca671e; /* Cor de fundo dos botões */
            color: white;
            border: none;
            transition: background-color 0.3s ease;
        }

        .btn-color2:hover {
            background-color: #dc7f37; /* Cor ao passar o mouse */
        }

        /* Estilo do cabeçalho */
        h1 {
            font-size: 2.5rem;
            font-weight: bold;
        }

        /* Ajustes adicionais para os links (botões) */
        a {
            text-decoration: none;
        }

        /* Container centralizado */
        .container {
            text-align: center;
            padding-top: 50px;
        }

        .d-grid {
            display: grid;
            gap: 20px;
        }
    </style>
</head>
<body>

    <%- include('./partials/menu') %>

    <div class="container">
        <h1 class="text-white">Bem-vindo ao Sistema de Estoque</h1>

        <br>
        <br>
        <br>
        
        <p class="text-white">Escolha uma opção:</p>
        
        <div class="d-grid gap-2 col-6 mx-auto">
            <a class="btn btn-color2" href="/fornecedores">Gerenciar Fornecedores</a>
            <a class="btn btn-color2" href="/vendas">Gerenciar Vendas</a>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
